% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose.R
\name{dk_decompose}
\alias{dk_decompose}
\title{Decompose Historical Debt Changes}
\usage{
dk_decompose(debt, interest_rate, gdp_growth, primary_balance, years = NULL)
}
\arguments{
\item{debt}{Numeric vector of historical debt-to-GDP ratios.}

\item{interest_rate}{Numeric vector of effective interest rates on
government debt. Must be the same length as \code{debt}.}

\item{gdp_growth}{Numeric vector of nominal GDP growth rates. Must be the
same length as \code{debt}.}

\item{primary_balance}{Numeric vector of primary balance-to-GDP ratios
(positive = surplus). Must be the same length as \code{debt}.}

\item{years}{Optional integer vector of year labels. Must be the same
length as \code{debt}. If \code{NULL} (default), years are numbered sequentially.}
}
\value{
An S3 object of class \code{dk_decomposition} containing:
\describe{
\item{data}{A \code{data.frame} with columns \code{year}, \code{debt}, \code{change},
\code{interest_effect}, \code{growth_effect}, \code{snowball_effect},
\code{primary_balance_effect}, and \code{sfa}.}
\item{years}{The year labels used.}
}
}
\description{
Breaks down observed year-on-year changes in the debt-to-GDP ratio into
four components:
}
\details{
\enumerate{
\item \strong{Interest effect}: \eqn{r_t / (1 + g_t) \cdot d_{t-1}}{r(t) / (1 + g(t)) * d(t-1)}
\item \strong{Growth effect}: \eqn{-g_t / (1 + g_t) \cdot d_{t-1}}{-g(t) / (1 + g(t)) * d(t-1)}
\item \strong{Primary balance effect}: \eqn{-pb_t}{-pb(t)}
\item \strong{Stock-flow adjustment (residual)}: actual change minus the sum of
the three identified components.
}

This is the standard decomposition used by the IMF (2013) and European
Commission. The SFA residual captures privatisation receipts, exchange-rate
valuation changes, below-the-line operations, and any measurement error.
}
\examples{
d <- dk_sample_data()
dec <- dk_decompose(
  debt = d$debt,
  interest_rate = d$interest_rate,
  gdp_growth = d$gdp_growth,
  primary_balance = d$primary_balance,
  years = d$years
)
dec
plot(dec)
}
\references{
Blanchard, O.J. (1990). Suggestions for a New Set of Fiscal Indicators.
\emph{OECD Economics Department Working Papers}, No. 79.
\doi{10.1787/budget-v2-art12-en}

International Monetary Fund (2013). \emph{Staff Guidance Note for Public Debt
Sustainability Analysis in Market-Access Countries}. IMF Policy Paper.
}
