% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fan_chart.R
\name{dk_fan_chart}
\alias{dk_fan_chart}
\title{Stochastic Debt Fan Chart}
\usage{
dk_fan_chart(
  debt,
  interest_rate,
  gdp_growth,
  primary_balance,
  shocks = NULL,
  shock_vcov = NULL,
  n_sim = 1000L,
  horizon = 5L,
  confidence = c(0.1, 0.25, 0.5, 0.75, 0.9),
  seed = NULL
)
}
\arguments{
\item{debt}{Numeric scalar; initial debt-to-GDP ratio.}

\item{interest_rate}{Numeric scalar or vector of length \code{horizon};
baseline interest rate path.}

\item{gdp_growth}{Numeric scalar or vector of length \code{horizon};
baseline GDP growth path.}

\item{primary_balance}{Numeric scalar or vector of length \code{horizon};
baseline primary balance-to-GDP path.}

\item{shocks}{A \code{dk_shocks} object (from \code{\link[=dk_estimate_shocks]{dk_estimate_shocks()}}) providing
the shock distribution, or \code{NULL}.}

\item{shock_vcov}{Optional 3x3 variance-covariance matrix (alternative to
\code{shocks}). Rows/columns ordered: growth, interest_rate, primary_balance.
Ignored if \code{shocks} is provided.}

\item{n_sim}{Integer; number of Monte Carlo simulations (default 1000).}

\item{horizon}{Integer; projection horizon in years (default 5).}

\item{confidence}{Numeric vector of quantile levels for fan bands
(default \code{c(0.10, 0.25, 0.50, 0.75, 0.90)}).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An S3 object of class \code{dk_fan} with components:
\describe{
\item{simulations}{Matrix of dimension \code{n_sim} x (\code{horizon} + 1)
containing all simulated debt paths.}
\item{quantiles}{Matrix of quantiles at each time step, with rows
corresponding to the \code{confidence} levels.}
\item{baseline}{Numeric vector of length \code{horizon} + 1; the
deterministic baseline debt path.}
\item{confidence}{The quantile levels used.}
\item{horizon}{The projection horizon.}
\item{prob_above}{Named list with the probability of debt exceeding
60 percent, 90 percent, and 120 percent of GDP at the terminal year.}
}
}
\description{
Projects debt-to-GDP paths via Monte Carlo simulation using the standard
debt dynamics equation. At each step, correlated shocks to growth, the
interest rate, and the primary balance are drawn from a multivariate
normal distribution and added to the baseline paths. The result is a fan
chart showing the distribution of projected debt paths.
}
\examples{
set.seed(1)
n <- 30
g <- rnorm(n, 0.02, 0.015)
r <- rnorm(n, 0.03, 0.01)
pb <- rnorm(n, -0.02, 0.01)
shocks <- dk_estimate_shocks(g, r, pb)

fan <- dk_fan_chart(
  debt = 0.90,
  interest_rate = 0.03,
  gdp_growth = 0.02,
  primary_balance = -0.02,
  shocks = shocks,
  n_sim = 500,
  horizon = 10,
  seed = 42
)
print(fan)

}
