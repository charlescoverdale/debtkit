% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{dk_project}
\alias{dk_project}
\title{Project Debt-to-GDP Path}
\usage{
dk_project(
  debt,
  interest_rate,
  gdp_growth,
  primary_balance,
  sfa = 0,
  horizon = 10,
  date = NULL
)
}
\arguments{
\item{debt}{Numeric scalar. Initial debt-to-GDP ratio (e.g., \code{0.90} for
90 per cent of GDP).}

\item{interest_rate}{Numeric scalar or vector of length \code{horizon}. Nominal
effective interest rate on government debt.}

\item{gdp_growth}{Numeric scalar or vector of length \code{horizon}. Nominal GDP
growth rate.}

\item{primary_balance}{Numeric scalar or vector of length \code{horizon}. Primary
balance as a share of GDP. Positive values denote a surplus; negative
values a deficit.}

\item{sfa}{Numeric scalar or vector of length \code{horizon}. Stock-flow
adjustment as a share of GDP. Default \code{0}.}

\item{horizon}{Integer scalar. Number of years to project forward. Default
\code{10}.}

\item{date}{Optional \code{Date}. If supplied, the projection is anchored to
this date (stored in the output for labelling purposes).}
}
\value{
An S3 object of class \code{dk_projection} containing:
\describe{
\item{debt_path}{Numeric vector of length \code{horizon + 1}, giving the
debt-to-GDP ratio from the initial period through the terminal period.}
\item{decomposition}{A \code{data.frame} with columns \code{year}, \code{debt},
\code{interest_effect}, \code{growth_effect}, \code{snowball_effect},
\code{primary_balance_effect}, \code{sfa_effect}, and \code{change}.}
\item{horizon}{The projection horizon.}
\item{inputs}{A list storing all input parameters.}
}
}
\description{
Projects a debt-to-GDP ratio forward using the standard debt dynamics
equation:
}
\details{
\deqn{d_{t+1} = \frac{1 + r_t}{1 + g_t} d_t - pb_t + sfa_t}{d(t+1) = [(1 + r(t)) / (1 + g(t))] * d(t) - pb(t) + sfa(t)}

where \eqn{d} is the debt-to-GDP ratio, \eqn{r} is the effective nominal
interest rate on government debt, \eqn{g} is nominal GDP growth,
\eqn{pb} is the primary balance as a share of GDP (positive = surplus),
and \eqn{sfa} captures stock-flow adjustments (e.g. privatisation receipts,
exchange-rate valuation changes, below-the-line operations).
}
\examples{
d <- dk_sample_data()
proj <- dk_project(
  debt = tail(d$debt, 1),
  interest_rate = 0.03,
  gdp_growth = 0.04,
  primary_balance = 0.01
)
proj
plot(proj)
}
\references{
Blanchard, O.J. (1990). Suggestions for a New Set of Fiscal Indicators.
\emph{OECD Economics Department Working Papers}, No. 79.
\doi{10.1787/budget-v2-art12-en}

International Monetary Fund (2013). \emph{Staff Guidance Note for Public Debt
Sustainability Analysis in Market-Access Countries}. IMF Policy Paper.
}
