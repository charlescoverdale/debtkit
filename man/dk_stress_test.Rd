% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_test.R
\name{dk_stress_test}
\alias{dk_stress_test}
\title{IMF Standardised Stress Tests}
\usage{
dk_stress_test(
  debt,
  interest_rate,
  gdp_growth,
  primary_balance,
  horizon = 5,
  growth_shock = -0.01,
  interest_shock = 0.02,
  exchange_shock = 0.15,
  fx_share = 0,
  pb_shock = -0.01,
  contingent_shock = 0.1,
  calibrate = NULL
)
}
\arguments{
\item{debt}{Numeric scalar. Initial debt-to-GDP ratio.}

\item{interest_rate}{Numeric scalar or vector of length \code{horizon}. Baseline
nominal effective interest rate.}

\item{gdp_growth}{Numeric scalar or vector of length \code{horizon}. Baseline
nominal GDP growth rate.}

\item{primary_balance}{Numeric scalar or vector of length \code{horizon}. Baseline
primary balance as a share of GDP (positive = surplus).}

\item{horizon}{Integer scalar. Projection horizon in years. Default \code{5}.}

\item{growth_shock}{Numeric scalar. Percentage-point reduction in GDP growth
applied in the first two years. Default \code{-0.01} (1 pp lower growth).}

\item{interest_shock}{Numeric scalar. Percentage-point increase in the
interest rate. Default \code{0.02} (200 basis points).}

\item{exchange_shock}{Numeric scalar. Depreciation fraction applied to
foreign-currency debt. Default \code{0.15} (15 per cent depreciation).}

\item{fx_share}{Numeric scalar. Share of debt denominated in foreign
currency. Default \code{0}.}

\item{pb_shock}{Numeric scalar. Percentage-point deterioration in primary
balance in the first two years. Default \code{-0.01}.}

\item{contingent_shock}{Numeric scalar. One-off increase in debt-to-GDP from
contingent liabilities materialising. Default \code{0.10}.}

\item{calibrate}{Optional named list for data-driven shock calibration.
Should contain numeric vectors \code{gdp_growth_hist}, \code{interest_rate_hist},
and \code{primary_balance_hist}. When provided, shock sizes are computed as
one standard deviation of each historical series, replacing the fixed
defaults. When \code{NULL} (default), the fixed defaults are used.}
}
\value{
An S3 object of class \code{dk_stress} containing:
\describe{
\item{scenarios}{A \code{data.frame} with columns \code{year}, \code{baseline}, \code{growth},
\code{interest_rate}, \code{exchange_rate}, \code{primary_balance}, \code{combined}, and
\code{contingent}.}
\item{terminal}{Named numeric vector of terminal debt-to-GDP under each
scenario.}
\item{inputs}{A list storing all input parameters.}
}
}
\description{
Applies six standardised IMF stress-test scenarios to a baseline debt
projection using the debt dynamics equation:
}
\details{
\deqn{d_{t+1} = \frac{1 + r_t}{1 + g_t} d_t - pb_t + sfa_t}{d(t+1) = [(1 + r(t)) / (1 + g(t))] * d(t) - pb(t) + sfa(t)}

The six scenarios are:
\enumerate{
\item \strong{Growth shock}: GDP growth reduced by \code{growth_shock} for the first
two years.
\item \strong{Interest rate shock}: interest rate increased by \code{interest_shock}
for the full horizon.
\item \strong{Exchange rate shock}: debt increases by
\code{debt * fx_share * exchange_shock} in year 1 (one-off stock-flow
adjustment from currency depreciation).
\item \strong{Primary balance shock}: primary balance reduced by \code{pb_shock} for
the first two years.
\item \strong{Combined shock}: simultaneous growth shock of \code{growth_shock / 2}
and interest rate shock of \code{interest_shock / 2}.
\item \strong{Contingent liabilities}: one-off debt increase of
\code{contingent_shock} in year 1.
}
}
\examples{
st <- dk_stress_test(
  debt = 0.90,
  interest_rate = 0.03,
  gdp_growth = 0.04,
  primary_balance = 0.01,
  fx_share = 0.20
)
st
plot(st)
}
\references{
International Monetary Fund (2013). \emph{Staff Guidance Note for Public Debt
Sustainability Analysis in Market-Access Countries}. IMF Policy Paper.

International Monetary Fund (2022). \emph{Staff Guidance Note on the Sovereign
Risk and Debt Sustainability Framework for Market Access Countries}.
IMF Policy Paper.
}
